const initialState = {
  type: 'visa',
  number: 3344223356878899,
  address: 'Minsk, Glavnaja st. 1, 1'
}

export default function card(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
